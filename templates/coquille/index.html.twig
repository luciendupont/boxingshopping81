{% extends 'base.html.twig' %}

{% block title %}COQUILLE{% endblock %}

{% block body %}

<div class="container">
    <div class="row align-items-start ">



        {% for coquille in coquilles %}







<div class="card mb-3 " with="auto">
    <div class="row align-items-center">
      <div class="col-4">
        <img src="boxe/article/{{coquille.imageArticle}}" class="img-fluid rounded-start" alt="...">
      </div>
      <div class="col-md-8">
        <div class="card-body">
          <h5 class="card-title">{{ coquille.nomArticle }}</h5>
          <p class="card-text">{{ coquille.descriptionArticle }}</p>
          <p class="card-text"> marque:{{ coquille.marqueArticle }}</p>
          <p class="card-text">Prix :{{ coquille.prixArticle }}€</p>
          <button type="button" class="btn btn-light" li class="nav-item">
                    <a class="nav-link" href="{{path('app_panier_add',{'id':coquille.id})}}">commandez</a></button>
       </div>
      </div>
    </div>
  </div>
{% endfor %}
    </div>
</div>
{% endblock %}
