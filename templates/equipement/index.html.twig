{% extends 'base.html.twig' %}

{% block title %}Hello EquipementController!{% endblock %}

{% block body %}
<div class="container text-center">
    <div class="row">
        <div class="col">
            <h1>BOXING  SHOPPING 1981</h1>

        </div>
    </div>




<div class="btn-btn" role="group" aria-label="Button group with nested dropdown">
    <a href="{{path('app_index')}}" class="btn btn-light">RETOUR</a>
    <div class="btn-group" role="group">

</div>
<div class="container">
    <div class="row">
        {% for cat in cats %}
        <div class="col-lg-4 my-2 d-flex justify-content-center">
            <div class="card border-dark mb-3" style="auto: 20rem;">
                <div class="card-header">{{cat.NomCategorie}}</div>
                <img src="boxe/{{cat.ImageArticle}}" alt="" width="100">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <button type="button" class="btn btn-light" li class="nav-item">
                    <a class="nav-link" href="{{path('app_gant')}}"> voir les gants</a></button>
      
                </div>
        </div>
        </div>


        {% endfor %}

        {% for cat1 in cats1 %}
        <div class="col-lg-4 my-2 d-flex justify-content-center">
            <div class="card border-dark mb-3" style="auto: 20rem;">
                <div class="card-header">{{cat1.NomCategorie}}</div>
                <img src="boxe/{{cat1.ImageArticle}}" alt="" width="100">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <button type="button" class="btn btn-light" li class="nav-item">
                    <a class="nav-link" href="{{path('app_short')}}">voir les shorts</a></button>
      
                </div>
        </div>
        </div>


        {% endfor %}

        {% for cat2 in cats2 %}
        <div class="col-lg-4 my-2 d-flex justify-content-center">
            <div class="card border-dark mb-3" style="auto: 20rem;">
                <div class="card-header">{{cat2.NomCategorie}}</div>
                <img src="boxe/{{cat2.ImageArticle}}" alt="" width="100">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <button type="button" class="btn btn-light" li class="nav-item">
                    <a class="nav-link" href="{{path('app_chaussure')}}">voir les chaussures</a></button>
      
                </div>
        </div>
        </div>
{% endfor %}



{% endblock %}
